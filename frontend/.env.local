# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Note: This URL is used by the frontend to communicate with the backend.
# Make sure the backend (FastAPI) is running on port 8000.
# If deploying, update this URL to match your backend URL.
